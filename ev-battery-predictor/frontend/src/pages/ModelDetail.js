import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ModelDetail = () => {
  return (
    <div>
      <Title level={2}>Model Details</Title>
      <p>Model detail page - Coming soon!</p>
    </div>
  );
};

export default ModelDetail;