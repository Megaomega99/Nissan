import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const Models = () => {
  return (
    <div>
      <Title level={2}>ML Models</Title>
      <p>ML Models page - Coming soon!</p>
    </div>
  );
};

export default Models;