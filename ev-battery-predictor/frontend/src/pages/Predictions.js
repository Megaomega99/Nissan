import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const Predictions = () => {
  return (
    <div>
      <Title level={2}>Predictions</Title>
      <p>Predictions page - Coming soon!</p>
    </div>
  );
};

export default Predictions;